function soma(a, b){
    return a + b;
}

const multiplicacao = function(x, y){
    return x + y;
}

const sum = (a, b) => a + b;

const object = () => ({ nome: 'Ygor', sobrenome: 'Verde' })

console.log(sum(10, 5));